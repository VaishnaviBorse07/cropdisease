services:
  - type: web
    name: cropdisease-app
    env: python
    branch: main
    buildCommand: "pip install -r python-scripts/requirements.txt"
    startCommand: "bash python-scripts/python app.py"
    plan: free

